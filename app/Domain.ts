export type SideQuest = {
  title: string;
  checked: boolean;
  chapter: number;
  index: number;
};

export type Weapon = {
  name: string;
  character: string;
  chapter: number;
  location: string;
  link: string;
};
